applications:
  - name: jaavaweb
    path: .
    buildpacks:
      -java _buildpack
    command: mvn clean package
     processes:
  - type: web
    instances: 1
    memory: 1024M
    disk_quota: 1024M
    random-route: true
